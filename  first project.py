def findtopic(
    import textbook
    a = textbook["tableofcontents"].len()
    for X in a:
        if tableofcontents[a] == "topic":
            c = topic.pagenumber
    b = textbook.len()
    for X in b:
        if X != c:
            turnpage()
        else:
            return(page)
)
def rating(
    if service == "good":
        total=total*.2
    else:
        total=total*.15
    if atmosphere == "good":
        stars=stars+1
    else:
        stars=stars-1
    if (food == "good"):
        stars=stars+1
    else:
        stars=stars-1
)
